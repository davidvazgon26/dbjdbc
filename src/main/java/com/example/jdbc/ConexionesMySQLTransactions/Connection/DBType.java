package com.example.jdbc.ConexionesMySQLTransactions.Connection;

public enum DBType {
    //Esto se hace para manejar varios tipos de conexiones, pero por el momento solo manejaremos la conexion a MySQL

    HSQLDB, MYSQL
}
